#!/usr/bin/env bash

export DIRENV_WARN_TIMEOUT=20s

# Self-contained Python devenv for the org-babel source blocks in this
# directory. Replaces the older setup that sourced ../python and ../rust
# siblings (those projects no longer exist).
eval "$(devenv direnvrc)"
use devenv
